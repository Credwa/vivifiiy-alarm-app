import { renderHook, act } from '@testing-library/react-hooks';
import useLoadAlarms from '@/hooks/useLoadAlarms.ts';

describe('useLoadAlarms test', () => {
  it('should load alarms from storage and set state', () => {});
});
